name: auth-service CI/CD

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

env:
  SERVICE_NAME: auth-service
  GO_VERSION: '1.25'

jobs:
  build-test:
    name: Build & Unit Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      # Gera o go.sum a partir do go.mod (o repositório não versiona o go.sum).
      # 'go mod tidy' resolve o grafo completo de dependências e escreve todas as
      # entradas que o 'go build' exige, evitando o erro "missing go.sum entry".
      - name: Sync dependencies (generate go.sum)
        run: go mod tidy

      - name: Build
        run: go build ./...

      - name: Run unit tests
        run: go test ./... -v

  lint:
    name: Linter / Static Analysis
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      # golangci-lint carrega os pacotes e também precisa do go.sum.
      - name: Sync dependencies (generate go.sum)
        run: go mod tidy

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v6
        with:
          version: latest
          args: --timeout=5m

  security-scan:
    name: Security Scan (SAST & SCA)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      # gosec compila o código e o Trivy SCA escaneia o go.sum: ambos precisam dele.
      - name: Sync dependencies (generate go.sum)
        run: go mod tidy

      # SCA: scans go.sum for known-vulnerable dependency versions
      - name: SCA - Trivy filesystem scan (dependency vulnerabilities)
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: fs
          scan-ref: .
          severity: CRITICAL
          exit-code: 1
          format: table

      # SAST: scans our own source code for insecure patterns
      - name: SAST - gosec (source code vulnerabilities)
        uses: securego/gosec@master
        with:
          args: '-severity high -confidence high ./...'

  docker-build-push:
    name: Docker Build & Push
    runs-on: ubuntu-latest
    needs: [build-test, lint, security-scan]
    steps:
      - uses: actions/checkout@v4

      - name: Set short commit SHA
        id: vars
        run: echo "sha_short=$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"

      - name: Build Docker image
        run: docker build -t ${{ env.SERVICE_NAME }}:${{ steps.vars.outputs.sha_short }} .

      - name: Container scan - Trivy image scan
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: ${{ env.SERVICE_NAME }}:${{ steps.vars.outputs.sha_short }}
          severity: CRITICAL
          exit-code: 1
          format: table

      # --- AWS / ECR push disabled: no AWS account access right now (switching accounts) ---
      # uncomment this whole block once AWS access is back to resume pushing images to ECR
      # - name: Configure AWS credentials
      #   if: github.ref == 'refs/heads/main' && github.event_name == 'push'
      #   uses: aws-actions/configure-aws-credentials@v4
      #   with:
      #     aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
      #     aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      #     aws-region: ${{ secrets.AWS_REGION }}
      #
      # - name: Login to Amazon ECR
      #   if: github.ref == 'refs/heads/main' && github.event_name == 'push'
      #   id: ecr-login
      #   uses: aws-actions/amazon-ecr-login@v2
      #
      # - name: Tag & push image to ECR
      #   if: github.ref == 'refs/heads/main' && github.event_name == 'push'
      #   env:
      #     ECR_REGISTRY: ${{ steps.ecr-login.outputs.registry }}
      #     IMAGE_TAG: v1.0.0-${{ steps.vars.outputs.sha_short }}
      #   run: |
      #     docker tag ${{ env.SERVICE_NAME }}:${{ steps.vars.outputs.sha_short }} $ECR_REGISTRY/${{ env.SERVICE_NAME }}:$IMAGE_TAG
      #     docker push $ECR_REGISTRY/${{ env.SERVICE_NAME }}:$IMAGE_TAG
      #
      # --- GitOps tag update disabled: depends on the ECR push above, still paused too ---
      # once AWS access is back AND the block above is uncommented, add here a step that
      # edits k8s/overlays/prod/patch-images.yaml in the root repo with the new IMAGE_TAG
      # and commits+pushes it, so ArgoCD picks up the change automatically.
